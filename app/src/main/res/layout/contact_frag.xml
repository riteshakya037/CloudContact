<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <it.neokree.materialtabs.MaterialTabHost
        android:id="@+id/materialTabHost"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        app:accentColor="@android:color/white"
        app:primaryColor="@color/primaryColor"
        app:textColor="#FFFFFF" />

    <android.support.v4.view.ViewPager xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/tabPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/materialTabHost" />

    <View
        android:layout_width="match_parent"
        android:layout_height="5dp"
        android:layout_below="@id/materialTabHost"
        android:background="@drawable/toolbar_shadow" />

    <!--<android.support.v4.view.PagerTitleStrip--><!--android:id="@+id/routinePageTitle"--><!--style="@style/viewPagerTitleStrip"--><!--android:fontFamily="sans-serif-thin"--><!--/>-->
</RelativeLayout>